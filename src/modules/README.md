# modules

The flat registry of carousel cards: one entry per service or project, in carousel
order, with its display label, accent colour and placeholder glyph. Phase 3 attaches
real data sources here; until then a card shows only its name and glyph.
