# rendering

How a frame gets drawn: the React Three Fiber canvas and renderer settings, the colour
palette, post-processing (bloom, vignette) and adaptive quality. Nothing in here knows
what objects are in the world; that is `scene-graph`.
