# components

React DOM components: the client boundary that mounts the canvas, fallbacks, and the
`hud/` overlay (today only the tracking indicator, the seed of the final HUD). Nothing here renders inside the Three.js scene; that is `scene-graph`.
